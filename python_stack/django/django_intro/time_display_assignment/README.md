# Time Display Assignment

A simple Django app that displays the current date and time.

## Setup

```bash
pip install django
python manage.py runserver
```

Then visit `http://127.0.0.1:8000/` in your browser.

## Features

- Displays the current date and time on page load
- Styled with a custom CSS file
